# ng2-charts declares an @angular/cdk peer (used only by its schematics, not at
# runtime) whose latest resolves to a version targeting a newer Angular major,
# which conflicts with this project's Angular 21. legacy-peer-deps makes npm skip
# that peer resolution so installs are reproducible locally, in CI, and on Vercel.
legacy-peer-deps=true
